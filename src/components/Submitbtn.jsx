import React from 'react'

const Submitbtn = ({ text }) => {
    return (
        <button className='submit-btn' >
            <span className="btn"><p >{text}</p></span>
        </button>
    )
}

export default Submitbtn